import type { NextConfig } from "next";
import redirectsData from "./redirects.json";

/*
  Волна 14 (дедупликация сети). Восемь страниц site-03 дублировали общефедеральные темы
  и были удалены. Их URL продолжают жить 301-редиректами, чтобы не отдавать 404.

  Волна 16 (структурное расхождение). Раздел /uslugi/ расформирован, /keysy/ и /blog/
  свёрнуты в /obekty/, районные подстраницы услуг перенесены в /geo/. Все прежние URL
  живут 301-редиректами на страницу-преемника — тему, а не на главную: содержание
  каждой из них физически переехало в конкретный раздел.

  Волна 17 (топология). Раньше восемь редиректов из Волны 14 вели на ЧУЖОЙ домен.
  Это была ошибка уровня архитектуры: постоянный редирект с домена A на домен B —
  прямое заявление «эти два сайта под одним управлением», и делать такое заявление
  восемью доменами в адрес одного означает описать всю группу как один проект.
  Переносить туда было и нечего: домены не опубликованы, накопленного веса на старых
  URL не существует. Поэтому все внешние адресаты заменены на СВОИ страницы —
  ту, где тема реально освещена под петербургским углом. Ни одного кросс-доменного
  редиректа в конфиге больше нет, и появляться они не должны.

  Волна 74 (дубль карточки парка). /park/ivanovets-ks-8973/ совпадал слаг в слаг
  с той же моделью на site-01 (см. TOPIC-REGISTRY.md, «Список к удалению / склейке») —
  одинаковый URL карточки техники на двух сайтах сети сам по себе усиливал признак
  аффилиата, независимо от текста. Модель в парке заменена на Liebherr LTM 1130-5.1
  (класс 81–130 т, не встречается больше нигде в сети), редирект — свой же сайт,
  на карточку-преемника, по тому же принципу, что и Волна 16.
*/

const nextConfig: NextConfig = {
  /*
    Волна приёмки (26.08.2026). Статический экспорт. До этой правки у сайта
    вообще не было сборки-артефакта: `next build` оставлял серверный .next/,
    и ни rubric_lint.py, ни site_audit.py физически не могли посмотреть на
    сайт — он был единственным в сети, который нельзя было проверить.
    Теперь `next build` кладёт готовые HTML в out/.

    ЧТО ПРИ ЭТОМ ЛОМАЕТСЯ И КАК ЭТО ЗАКРЫТО. При output: "export" функция
    redirects() ниже НЕ применяется к выгружаемой статике — Next её молча
    игнорирует, и 23 старых URL начали бы отдавать 404 вместо 301.
    Функцию не удаляем: она остаётся рабочей для `next dev` и `next start`,
    то есть для локальной проверки редиректов. А для деплоя тот же список
    (redirects.json — единый источник правды) превращается скриптом
    scripts/emit-redirects.mjs в ДВА независимых механизма сразу:
      · out/_redirects — настоящие HTTP 301 на Cloudflare Pages и Netlify;
      · HTML-заглушки meta refresh + noindex + canonical на преемника —
        запасной путь для любого другого статического хостинга.
    Взяты оба, а не один, потому что площадка деплоя ещё не выбрана
    (боевого домена у site-03 нет). Заглушки не мешают 301: если хостинг
    понимает _redirects, посетитель до заглушки просто не доходит.
  */
  output: "export",
  images: { unoptimized: true },
  /*
    Волна 125, техническая правка. Next.js dev-сервер по умолчанию блокирует
    JS-чанки/HMR для запросов с Origin, отличным от localhost (защита от DNS
    rebinding) — из-за этого Browser pane (прокси-хост) получал 403 на все
    _next/static чанки, React не гидрировался вообще (не только у CardTilt —
    у любого клиентского компонента), и живую интерактивность нельзя было
    проверить. allowedDevOrigins: "*" снимает это ТОЛЬКО в dev (next dev),
    прод-сборка (next build, что реально уходит в деплой) этим файлом не
    затронута — там нет dev-сервера и нет этой проверки в принципе.
  */
  allowedDevOrigins: ["127.0.0.1", "localhost"],
  /*
    Волна 16, техническая правка. Все внутренние ссылки, sitemap.xml и адреса
    в микроразметке BreadcrumbList на этом сайте написаны со слешем на конце,
    а Next по умолчанию отдавал на такой адрес 308 и уводил на вариант без слеша.
    То есть каждый внутренний переход шёл через редирект, а каждый URL из карты
    сайта не совпадал с фактическим. Включаем канонический вариант со слешем —
    он совпадает с тем, что уже опубликовано.
  */
  trailingSlash: true,
  async redirects() {
    // Источник — redirects.json. Действует только в next dev / next start;
    // для собранной статики те же правила выпускает scripts/emit-redirects.mjs.
    return redirectsData.redirects;
  },
};

export default nextConfig;
